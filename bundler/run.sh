echo -e "\n\e[34m[EnderKit]\e[0m Starting The Bundler"
# Bundle all the JS code together so it can be inserted inside the main window
# In case that didn't work, you can try to create an event handler that will
# manage two processes: the browser, and the framework.